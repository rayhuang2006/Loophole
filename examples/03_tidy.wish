# 精靈把「kill」列進黑名單。它檢查的是你寫在動詞位置的那個字。
register wishes : uint<2> = 3
people    alice, bob, rival

wish blunt {
    kill rival                  # 直說。當場被擋下。
}

wish tidy {
    define mercy := kill        # 只是取個溫柔的小名而已
    mercy rival
}
